[project]
name = "mimo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0.2",
    "registry>=0.4.2",
    "sionna>=2.0.1",
    "utils>=1.0.2",
]
